---
const { label } = Astro.props;
---

<div class="mt-5 flex w-full items-center justify-center px-4 opacity-80">
  <div class="flex-1 border-t border-[salmon]"></div>
  <span class="mx-4 text-xl font-bold text-[salmon]">{label}</span>
  <div class="flex-1 border-t border-[salmon]"></div>
</div>
