---
export interface Props {
  year: number;
  uniqueYears: number[];
}

const { year, uniqueYears } = Astro.props;
const nextYear = uniqueYears.find((y: number) => y > year);
const prevYear = [...uniqueYears].reverse().find((y) => y < year);
---

<div class="flex justify-center gap-4 p-2 text-center text-3xl font-black sm:text-4xl">
  <div class="w-[4ch]">
    {
      prevYear && (
        <a class="text-neutral-500 transition hover:text-[lightcoral] hover:underline" href={`/${prevYear}`}>
          {prevYear}
        </a>
      )
    }
  </div>
  <div class="w-[4ch]">
    <h2 class="text-[lightcoral] underline">{year}</h2>
  </div>
  <div class="w-[4ch]">
    {
      nextYear && (
        <a class="text-neutral-500 transition hover:text-[lightcoral] hover:underline" href={`/${nextYear}`}>
          {nextYear}
        </a>
      )
    }
  </div>
</div>
